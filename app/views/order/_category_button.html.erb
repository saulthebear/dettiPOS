<div class="col mb-3">
  <button class="btn btn-lg btn-outline-primary w-100 js-category"
    data-category-id="<%= category.id %>"
    data-level="<%= level %>"
    data-ancestors="<%= ancestors %>"
  >
    <h4><%= category.name %></h3>
  </button>
</div>